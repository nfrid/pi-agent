// Canonical activity model. Keep this compatibility module so Pi's extension
// loader can resolve the historical path without maintaining a second copy.
export * from '@pi-dashboard/activity-model';
