// Public shared-model entry point for consumers that already load the TUI extension.
// The implementation remains local for Pi's extension loader compatibility.
export * from '../../packages/activity-model/src/index';
