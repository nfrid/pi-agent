/** Canonical tool-sequence renderer contract shared with the dashboard. */
export type {
  RendererContext,
  SequenceItem,
  SequenceOptions,
  SequenceRenderer,
  SequenceSnapshot,
} from '@pi-dashboard/activity-model';
