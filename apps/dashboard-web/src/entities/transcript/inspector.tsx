/** Re-export shim — import from `./inspector/` folder. */
export {
  BoundedPayloadPreview,
  boundedInspectorText,
  normalizeToolResultText,
  StructuredPayloadView,
  ToolInspector,
  toolInspectorRecord,
  toolInspectorRows,
  toolPresentationKind,
  toolPreviewLanguage,
} from './inspector/index';
