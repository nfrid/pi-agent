diff --git a/dist/core/agent-session.js b/dist/core/agent-session.js
index ce8a9a2a613c08de483a6c94ef4f6341efff0457..c60cb774f417147a166bb2fa2cf37001033413d0 100644
--- a/dist/core/agent-session.js
+++ b/dist/core/agent-session.js
@@ -1903,6 +1903,7 @@ export class AgentSession {
                 }
                 void this.abort();
             },
+            abortCompaction: () => this.abortCompaction(),
             hasPendingMessages: () => this.pendingMessageCount > 0,
             shutdown: () => {
                 this._extensionShutdownHandler?.();
diff --git a/dist/core/extensions/runner.js b/dist/core/extensions/runner.js
index 418865ad891951e3fb7e2a65f4dca4d7d87e3895..ae883cbc820dfaf433c7452a28be2494e55e38dd 100644
--- a/dist/core/extensions/runner.js
+++ b/dist/core/extensions/runner.js
@@ -133,6 +133,7 @@ export class ExtensionRunner {
     getSignalFn = () => undefined;
     waitForIdleFn = async () => { };
     abortFn = () => { };
+    abortCompactionFn = () => { };
     hasPendingMessagesFn = () => false;
     getContextUsageFn = () => undefined;
     compactFn = () => { };
@@ -178,6 +179,7 @@ export class ExtensionRunner {
         this.isProjectTrustedFn = contextActions.isProjectTrusted;
         this.getSignalFn = contextActions.getSignal;
         this.abortFn = contextActions.abort;
+        this.abortCompactionFn = contextActions.abortCompaction;
         this.hasPendingMessagesFn = contextActions.hasPendingMessages;
         this.shutdownHandler = contextActions.shutdown;
         this.getContextUsageFn = contextActions.getContextUsage;
@@ -513,6 +515,10 @@ export class ExtensionRunner {
                 runner.assertActive();
                 runner.abortFn();
             },
+            abortCompaction: () => {
+                runner.assertActive();
+                runner.abortCompactionFn();
+            },
             hasPendingMessages: () => {
                 runner.assertActive();
                 return runner.hasPendingMessagesFn();
diff --git a/dist/core/extensions/types.d.ts b/dist/core/extensions/types.d.ts
index cb7b8e7cfb9d1fb07f4f8c3161d25927b3186e94..6064b2b1a6ef1a73219b881b4f8835980448a9d0 100644
--- a/dist/core/extensions/types.d.ts
+++ b/dist/core/extensions/types.d.ts
@@ -236,6 +236,8 @@ export interface ExtensionContext {
     signal: AbortSignal | undefined;
     /** Abort the current agent operation */
     abort(): void;
+    /** Abort the active context compaction, if any. */
+    abortCompaction(): void;
     /** Whether there are queued messages waiting */
     hasPendingMessages(): boolean;
     /** Gracefully shutdown pi and exit. Available in all contexts. */
@@ -1214,6 +1216,7 @@ export interface ExtensionContextActions {
     isProjectTrusted: () => boolean;
     getSignal: () => AbortSignal | undefined;
     abort: () => void;
+    abortCompaction: () => void;
     hasPendingMessages: () => boolean;
     shutdown: () => void;
     getContextUsage: () => ContextUsage | undefined;
